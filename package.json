{
  "name": "horizon-app",
  "version": "1.0.0",
  "description": "command line tool that takes flags for sums/products of numbers",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.9.0"
  },
  "devDependencies": {
    "mocha": "^3.2.0"
  }
}

"scripts": {
    "test": "mocha"
  }
